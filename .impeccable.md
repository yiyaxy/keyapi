# Design Context — new-api (frontend rewrite)

> 本文件是**新前端重写**的设计 context（who / why / brand）。
> **所有 token / 组件 / 可视规则的真源是 `design_file/`**——本文件不复述，只指路。
> `web/.impeccable.md` 适用于既有 Semi UI 版本；新旧各管一栈，不合并。

## Authoritative design sources（真源，按优先级）

1. **`design_file/colors_and_type.css`** — 所有 token（OKLCH 色板、明暗主题、type 标尺、间距、圆角、shadow、motion）唯一定义处。**不要在别处重定义或硬编码 hex。**
2. **`design_file/fonts/fonts.css`** — Web 字体加载器（当前 Be Vietnam Pro / JetBrains Mono 替身，真字体 woff2 待补）。
3. **`design_file/README.md`** — 完整设计说明（voice/tone、number format、icon 规则、反模式清单）。任何非琐碎设计工作前先读。
4. **`design_file/SKILL.md`** — 速查规则（do / don't）。
5. **`design_file/ui_kits/console/`** — hi-fi 参考 JSX（Dashboard / Keys / Playground / Billing + Sidebar / Topbar）。**作参考不直接用**：新前端用 shadcn/Radix 重做，但吃同一套 token。
6. **`design_file/preview/*.html`** — 19 张 spec card（按 color / type / spacing / components 分类）。查表字典。
7. **`design_file/screenshots/`** — 目标视觉，样式对齐参照。

## Target Stack（锁定，不回头）

- Vite 6 + React 19 + TypeScript 5 strict
- UI 基座：**shadcn/ui + Radix primitives + Tailwind 3**（未来可升 v4）
- Tailwind `theme.extend` 映射到 design_file CSS variables（`var(--bg-0)` / `var(--accent)` 等）
- 服务端状态：TanStack Query；客户端状态：Zustand
- 表单：react-hook-form + zod
- 表格：TanStack Table（headless）
- 图表：recharts，吃 design_file 的 `--data-0..7`
- i18n：react-i18next，zh 源，**仅 zh / en 双语**
- Markdown / 代码高亮：react-markdown + shiki
- SSE（playground）：原生 fetch + ReadableStream
- 图标：**Lucide only**（1.5 stroke，16 / 18–20px），vendor 品牌标走 `@lobehub/icons`
- 包管理：**bun**
- 构建产物仍进 `web-next/dist`（初期），切流量时改 Go `//go:embed` 路径

## Design Context

### Users

核心画像：**付费的开发者型用户**——个人买家或小团队管理员。被 OpenAI Platform / Anthropic Console / Linear / Vercel / Stripe 后台训练过眼睛，看到土味后台会当场关闭。

典型任务：注册 → 首次充值/订阅 → 开 API key → 跑 playground → 查余额/账单 → 申请开票。

心态：花真金白银买 LLM 额度，对**价格透明度**、**支付反馈**、**余额准确性**高度敏感。

### Jobs-to-be-done

1. 60 秒内看懂"剩多少 / 花了多少 / 下一笔何时扣"
2. 一键完成充值、切换订阅、开票
3. 拿到可用的 API key 和最小可跑接入片段
4. 观测调用日志与异常，快速定位问题
5. 多租户管理员：查成员、计费、额度、告警

### Brand Personality

**三词：技术 · 锐利 · 现代。**

- 像 developer tool，不像"企业管理系统"
- 文案动词优先、短促、sentence case；**零 emoji**、零客服话术（禁"尊敬的用户""小主""亲"）
- 数字是视觉主角；英文术语保留（API Key / Token / Quota / Playground / Channel / Tenant）不翻译
- 情绪目标：**"这钱花得放心、花得专业。"** 信心 > 热情；克制 > 讨好

详见 `design_file/README.md` → Content Fundamentals（voice、tone-by-surface、casing、number format）。

### Aesthetic Direction

**正参考**（按权重降序）：Anthropic Console > Linear > OpenAI Platform > Vercel Dashboard > Stripe Dashboard > Resend / Railway

**反参考**（绝不要像）：
- 花哨 Web3 渐变 / 玻璃拟态满屏浮动
- 传统 SaaS 后台：拥挤顶栏、彩虹 tag、卡通圆角图标
- 国风管理系统：大红大金、报表堆叠
- Notion / Slack 聊天风气泡审美

主题：明暗双主题，跟随系统，明为默认。所有颜色在两套主题下都走 `design_file/colors_and_type.css` 的 token——不自定义、不跳过 token。

### Accessibility

**目标：WCAG 2.2 AA + `prefers-reduced-motion` + 色盲安全。**

- 文字对比度 ≥ 4.5:1，大元素 ≥ 3:1（明暗两套都查）
- 交互元素全键盘可达；focus-ring 用 `var(--focus-ring)`（独立冷青），不与 `--accent` 同色
- 尊重 `prefers-reduced-motion: reduce`：`colors_and_type.css` 已全局置零 transition
- 状态永远 `dot + label + soft background`，不依赖纯色（danger / warn / success / info 均配 icon + 文字）
- 表单错误用 `aria-invalid` + `aria-describedby`，不光红边框

### Design Principles（与 design_file SKILL.md 同步）

1. **Money is the hero.** 余额 / 消耗 / 价格永远第一视觉焦点——`.money` / `.num` 类，`tabular-nums + cv11 + ss01`，字重 600。
2. **Token over hex.** 用 `var(--*)`，不用 `#rrggbb`；不用内联 `style` 写死色值。
3. **One primary per screen.** 每屏一个主 CTA（近黑 `--primary`），其余降级 ghost / secondary / text。
4. **One chromatic accent.** 整个产品只有一个彩色 `--accent`（graphite-sage green）——链接、选中、focused control、一屏唯一的"关键数字"。**不装饰使用。**
5. **Density with air.** 信息密度高，但只用 12 / 16 / 24 三档间距和 `bg-0 → bg-1 → bg-2 → bg-3` 背景层级——不堆阴影，不堆边框。
6. **Dual-theme parity.** 明暗两套都查对比度；图表用 `--data-n` 语义序列，不写死 hex。
7. **i18n by default.** 所有可见文案进 `react-i18next`，zh 为源；术语表保留英文不翻译。
8. **Radix first, custom last.** 交互语义走 Radix / shadcn；新组件前先翻 `web-next/src/components/ui/`。
9. **No AI tells.** 禁左/右色条 border、禁渐变文字 / `background-clip: text`、禁 hero metric 大号渐变模板、禁 sparkline 装饰、禁 glassmorphism、禁 backdrop blur on cards、禁 emoji。

## Open questions

- 真字体 woff2（General Sans / Commit Mono）到位时间——在那之前用 design_file 的 Google Fonts 替身
- 打包策略：CDN 加载字体 vs woff2 自托管；国内访问稳定性需测
- Logo 现有 `logo.png` 是彩色渐变，与"no gradients"规则冲突——视作遗留，待重设计
- Marketing / pricing 公开站是否纳入 web-next，或另起一套（当前 design_file 只覆盖 authenticated console）
