# 用户使用文档

这份文档面向平台用户、分销商和租户管理员。你可以把它理解成一份后台操作手册：先教普通用户怎么把模型接到自己的工具里，再介绍分销商如何配置自己的租户、管理用户、配置充值和返利。

## 1. 先把模型用起来

大多数用户最关心的是 3 个信息：`Base URL`、`模型名称`、`API Key`。只要把这 3 个填到支持 OpenAI 接口格式的工具里，就可以开始使用模型。

### 1.1 Base URL 怎么填

在你的客户端里选择“OpenAI 兼容接口”或“自定义 OpenAI 接口”，然后填写平台提供的接口地址。

常见填写方式：

```text
https://你的平台域名/v1
```

如果你的平台域名是：

```text
https://api.example.com
```

那么 `Base URL` 一般填写：

```text
https://api.example.com/v1
```

注意：不要只填网站后台地址，也不要漏掉 `/v1`。如果你不确定自己的域名，请联系平台管理员或分销商。

### 1.2 模型名称怎么填

模型名称就是后台展示的模型 ID，例如：

```text
gpt-4o-mini
claude-3-5-sonnet
gemini-2.0-flash
deepseek-chat
```

具体能用哪些模型，以你的后台“模型 / 模型广场 / 可用模型”里展示的名称为准。复制模型名称时建议完整复制，不要手动改大小写或符号。

### 1.3 API Key 怎么获取

登录后台后，进入：

```text
API Key 管理
```

点击“新建 Key”或复制已有 Key，把它填到客户端的 `API Key` 位置。

Key 通常长这样：

```text
sk-xxxxxxxxxxxxxxxx
```

后续平台会调整为：新注册用户默认自动获得一个包含所有平台模型的 Key。也就是说，用户注册后可以直接复制默认 Key 使用，不需要自己先创建。

### 1.4 在客户端里怎么填

以常见 AI 客户端为例，填写方式如下：

```text
接口类型：OpenAI Compatible / OpenAI 兼容
Base URL：https://你的平台域名/v1
API Key：你的 sk- 开头密钥
Model：后台展示的模型名称
```

保存后，可以发送一句简单测试：

```text
你好，请用一句话介绍你自己。
```

如果返回正常，说明配置成功。

## 2. 后台主要功能

后台分为普通用户功能和租户管理员功能。普通用户主要使用模型、查看余额、充值、查看消费记录；分销商和租户管理员则需要配置自己的租户、模型、用户、充值和返利规则。

## 3. 普通用户常用功能

### 3.1 查看余额

登录后台后，可以在首页或仪表盘查看当前余额。调用模型会消耗余额，不同模型、不同倍率的消耗可能不同。

### 3.2 管理 API Key

进入 `API Key 管理`，可以创建、复制、编辑、禁用或删除 Key。

建议：

- 一个客户端使用一个独立 Key。
- 不要把 Key 发给陌生人。
- Key 泄露后请立即删除并重新创建。

### 3.3 充值

进入 `充值` 页面，选择充值金额并完成支付。支付成功后余额会自动到账。

如果支付成功但余额没有变化，可以查看订单状态，或联系管理员处理补单。

### 3.4 查看消费记录

进入 `消费记录` 页面，可以查看每次调用的模型、时间、消耗额度等信息。排查余额消耗问题时，优先查看这里。

### 3.5 邀请与返利

用户可以在 `邀请中心` 查看自己的邀请码、邀请人数、可提现/可转换的返利余额。

被邀请用户绑定邀请码或注册后，邀请人可能获得奖励；被邀请用户后续充值时，邀请人也可能按规则获得返利。

## 4. 分销商 / 租户管理员功能

分销商通常会拥有自己的租户。租户可以理解为你自己的独立站点或业务空间，你可以管理自己的用户、模型、充值方式和返利策略。

### 4.1 租户信息

进入租户相关页面后，可以查看当前租户的基础信息，例如租户名称、标识、状态等。

建议先确认：

- 当前登录的是不是自己的租户。
- 租户域名是否正确。
- 租户状态是否正常。

### 4.2 租户配置

租户配置用于控制你站点里的常用开关和业务参数，例如注册开关、登录方式、默认额度、充值设置、邀请奖励等。

常见配置包括：

- 是否允许用户注册。
- 新用户注册默认赠送额度。
- 邀请人注册奖励。
- 被邀请人奖励。
- 充值返利次数。
- 充值返利比例。
- 订阅返利次数。

修改配置后，建议用一个测试账号完整走一遍注册、充值和调用模型流程。

### 4.3 用户管理

进入 `用户管理`，可以查看当前租户下的用户列表。

管理员可以进行：

- 创建用户。
- 修改用户显示名、邮箱、分组、余额。
- 设置用户等级。
- 启用或禁用用户。
- 提升或降低管理员权限。
- 删除用户或移出租户。

其中“用户等级”会影响邀请返利和充值返利。不同等级可以设置不同奖励规则。

### 4.4 用户等级与返利

进入 `邀请返利设置`，可以配置用户等级。

每个等级可以设置：

- 邀请人注册奖励：邀请成功后，上级获得多少奖励。
- 被邀请人奖励：新用户绑定邀请码或注册后，自己获得多少奖励。
- 充值返利次数：被邀请人前几次充值会给上级返利。
- 充值返利比例：按充值额度的百分比返利给上级。
- 订阅返利次数：如果平台启用订阅套餐，可控制订阅返利次数。

返利规则优先级：

```text
单个邀请人自定义配置 > 用户等级配置 > 租户默认配置
```

也就是说，如果某个邀请人单独配置了返利规则，会优先生效；如果没有单独配置，则使用他所属用户等级的规则；如果也没有等级，则使用租户默认规则。

### 4.5 充值和订单管理

租户管理员可以查看用户充值订单，包括待支付、已支付、失败、过期等状态。

常见操作：

- 查看订单号和支付状态。
- 查询用户充值记录。
- 对异常订单进行补单。
- 删除无效订单。

补单前请确认用户确实已经付款，避免误加余额。

### 4.6 模型与渠道管理

模型和渠道决定用户能调用哪些 AI 模型，以及这些请求会转发到哪个上游平台。

常见概念：

- 模型：用户在客户端里填写的模型名称。
- 渠道：后台连接的上游服务商，例如 OpenAI、Claude、Gemini、DeepSeek 等。
- 分组：用于控制不同用户或 Key 能使用哪些模型/渠道。
- 倍率：用于控制不同模型或渠道的计费消耗。

建议配置顺序：

1. 先确认上游平台 Key 可用。
2. 新建或编辑渠道，填写上游 Key 和支持的模型。
3. 测试渠道是否可用。
4. 确认模型名称在前台展示正常。
5. 用测试用户的 Key 调用模型。

### 4.7 订阅套餐

如果租户启用了套餐能力，可以配置不同订阅计划，例如月套餐、年套餐、会员套餐。

套餐通常可以设置：

- 套餐价格。
- 套餐额度。
- 有效期。
- 购买后升级到哪个用户分组。
- 每个用户最多购买次数。

### 4.8 发票和工单

如果平台启用了发票和工单功能，用户可以提交发票申请或问题工单，管理员可以在后台处理。

常见用途：

- 用户充值后申请发票。
- 用户反馈模型不可用。
- 用户反馈充值未到账。
- 用户提交账号或 Key 问题。

### 4.9 数据统计

后台提供用量、充值、用户增长、邀请返利等统计数据。分销商可以通过这些页面了解自己的租户运营情况。

重点关注：

- 活跃用户数量。
- 充值金额。
- 模型调用量。
- 消耗额度。
- 邀请用户数量。
- 返利支出。

## 5. 小程序端功能

小程序端面向普通用户，主要提供快捷使用入口。

用户可以在小程序里：

- 查看余额。
- 充值。
- 查看订单。
- 查看消费记录。
- 查看邀请码。
- 绑定邀请码。
- 查看邀请收益。

在 `我的` 页面可以进入“绑定邀请码”。绑定成功后，后续充值会按后台规则返利给上级邀请人。

注意：邀请码绑定后不可随意修改，请确认邀请码无误后再绑定。

## 6. 常见问题

### 6.1 客户端提示 401 或认证失败

请检查：

- API Key 是否复制完整。
- Key 前后是否有空格。
- Base URL 是否带 `/v1`。
- Key 是否被禁用或删除。
- 当前账号是否还有余额。

### 6.2 模型不存在

请检查：

- 模型名称是否复制正确。
- 当前 Key 是否有权限使用该模型。
- 当前用户分组是否能访问对应模型。
- 后台渠道是否启用并支持该模型。

### 6.3 充值后没有到账

请先查看订单状态。如果订单已支付但余额未到账，请联系租户管理员处理补单。

### 6.4 邀请返利没有到账

请检查：

- 被邀请人是否已经绑定邀请码或通过邀请码注册。
- 邀请人是否有对应等级或返利配置。
- 充值返利次数是否已经用完。
- 返利比例是否设置为 0。
- 订单是否已支付成功。

### 6.5 分销商应该先配置什么

建议按这个顺序配置：

1. 配置租户基础信息和域名。
2. 配置模型渠道，确认模型能正常调用。
3. 配置充值方式和最小充值金额。
4. 配置用户等级和返利规则。
5. 创建测试用户并完成注册、绑定邀请码、充值、调用模型。
6. 确认无误后再正式推广。

## 7. 安全建议

- 不要公开后台管理员账号。
- 不要把上游平台 Key 发给用户。
- 用户只需要使用本平台生成的 API Key。
- 定期检查异常消费记录。
- 发现 Key 泄露后立即删除。
- 调整返利比例前，先计算好成本和利润。

## 8. 快速检查清单

上线前请确认：

- Base URL 可以访问。
- 默认 API Key 可以正常调用模型。
- 至少一个模型测试成功。
- 用户注册流程正常。
- 充值流程正常。
- 邀请码绑定正常。
- 充值返利规则正确。
- 管理员能查看订单和消费记录。

