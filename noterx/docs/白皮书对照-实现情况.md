# 薯医 NoteRx — 白皮书对照：已实现 / 未做 / 未完成

本文档按 [`noterx_whitepaper.md`](../noterx_whitepaper.md) 的叙事与功能拆解，对照**当前仓库代码与工程状态**（以 `yxz` 分支及合并目标为准），说明哪些已落地、哪些是部分完成、哪些尚未做或需人工补齐。

*说明：白皮书中的队名、路演话术、评审自评等不属代码范畴，此处仅标注「是否需在材料中单独准备」。*

---

## 一、命名与品牌

| 白皮书内容 | 状态 | 说明 |
|------------|------|------|
| 产品名 NoteRx / 薯医 | **已做** | README、前端文案、导出卡片等已统一 |
| Slogan「你的笔记，值得被看见」 | **已做** | 首页、报告页等可见 |
| 队名 PageOne、队伍介绍 | **非代码** | 需在 Slide / 口头介绍中体现 |

---

## 二、核心卖点 vs 实现

### 卖点 1：真实痛点（创作者诊断工具）

| 白皮书内容 | 状态 | 说明 |
|------------|------|------|
| 系统性诊断（封面 / 标题 / 标签 / 时间等） | **已做** | 多 Agent + baseline 对比 + 前端完整流程 |

### 卖点 2：数据科学 — Baseline「真实数据」护城河

| 白皮书内容 | 状态 | 说明 |
|------------|------|------|
| 2–3 个垂类（美食 / 穿搭 / 科技） | **已做且扩展** | 代码侧现为 **6 个垂类**（含旅行、美妆、健身），与白皮书「先做 3 垂类」不冲突，演示更丰富 |
| 每垂类 **2000–3000 条真实笔记** 采集 | **未做** | 当前 `scripts/seed_data.py` 为 **合成数据**，量级约每垂类 500 条（共 3000），答辩若强调「真实数据」需与叙事对齐或补足采集 |
| 标题字数、关键词、封面特征、标签与互动、发布时间、粉丝量等 baseline | **部分完成** | **脚本层**：`compute_baseline.py` 含发布时间分布、粉丝分层、标签分桶等；**数据真实性**依赖是否替换为真实采集 |
| 用户笔记与「真实数据」对比（非纯 AI 感觉） | **已做** | `BaselineComparator` + Agent prompt 注入对比结果；对比对象是 **当前 DB 中的统计**（现为合成） |

### 卖点 3：多 Agent 辩论

| 白皮书内容 | 状态 | 说明 |
|------------|------|------|
| 内容 / 视觉 / 增长 / 用户模拟 / 综合裁判 五类 Agent | **已做** | `backend/app/agents/` + `orchestrator.py` |
| 并行第一轮 + 辩论轮 + 裁判 | **已做** | `asyncio.gather` + `_run_debate` + `JudgeAgent` |
| 辩论过程可展示给用户 | **已做** | API `debate_timeline` + 前端 `AgentDebate` 时间线 |
| 「视觉说好、用户模拟说不喜欢」类冲突展示 | **依赖 LLM 输出** | 架构支持；具体话术质量依赖 prompt 与模型 |

### 卖点 4：诊断卡片即传播素材

| 白皮书内容 | 状态 | 说明 |
|------------|------|------|
| 雷达图 + 问题 + 建议的可分享卡片 | **已做** | `html2canvas` 导出 `DiagnoseCard` |

### 卖点 5：团队即用户（真实博主）

| 白皮书内容 | 状态 | 说明 |
|------------|------|------|
| 陈宇夏等真实运营经历 | **非代码** | Slide / 路演时使用 |

---

## 三、产品功能拆解（对照功能表）

| 模块（白皮书优先级） | 状态 | 说明 |
|---------------------|------|------|
| 笔记上传：截图 / 文字 / 链接（P0） | **已做** | 三 Tab；链接 `POST /api/parse-link`；截图可走 OCR（无标题时） |
| 封面诊断：构图、色彩、文字占比、人脸（P0） | **已做** | OpenCV / 回退启发式；已增强构图网格、色调、复杂度等 **叙述字段**；**无 CLIP 视觉语义**（见下文技术栈） |
| 标题诊断：关键词、长度、情绪、与垂类对比（P0） | **已做** | `text_analyzer.py`（jieba 等）+ baseline 对比进 Agent |
| 内容诊断：结构、密度、可读性（P0） | **已做** | 文本分析 + LLM Agent |
| 标签诊断：数量、热度、长尾（P1） | **已做** | baseline 含 top_tags、标签分桶；增长 Agent 使用 |
| 用户预测：AI 模拟评论区（P1） | **已做** | `UserSimAgent` + 报告区 `SimulatedComments` |
| Agent 辩论可视化（P1） | **已做** | `debate_timeline` + 折叠时间线 UI |
| 一键优化：改标题 / 正文 / 封面方向（P2） | **已做** | 裁判输出 `optimized_title`、`optimized_content`、`cover_direction`；前端可复制 |
| 诊断卡片导出（P0） | **已做** | 见上 |

可选流程「一键生成优化版笔记」白皮书写的是 **可选**；当前为 **裁判同请求内输出优化稿**，未单独拆「第二请求省钱」接口（属于可优化项，非阻塞）。

---

## 四、技术架构（对照第五节）

| 白皮书建议 | 状态 | 说明 |
|------------|------|------|
| 前端 Web App：上传 → 动画 → 报告 → 导出 | **已做** | React + **MUI**（白皮书写的是 Tailwind；现为 **设计实现偏差**，能力等价） |
| API Gateway（FastAPI） | **已做** | `main.py`、路由聚合 |
| OCR + 图像分析 + NLP + 数据对比 | **已做** | OCR 以 **视觉 LLM** 为主；NLP jieba；图像 OpenCV |
| 多 Agent 编排 | **已做** | `Orchestrator` |
| Baseline 本地 SQLite | **已做** | `backend/data/baseline.db`（gitignore，CI 内脚本生成） |
| **OpenCV + CLIP** | **部分** | **OpenCV / 传统特征：有**。**CLIP：未集成**（仍为后续清单 P1） |
| ECharts 雷达图 | **已做** | `RadarChart.tsx` |
| html2canvas 卡片 | **已做** | `DiagnoseCard.tsx` |
| LLM：Claude / GPT-4o | **已做** | `LLM_PROVIDER` + `OPENAI_BASE_URL` + `LLM_MODEL` 可切换兼容端 |

数据层篇幅写「美食×2000、穿搭×2000、科技×2000」：**结构支持，当前为合成 seed，非真实 2000×3 采集。**

---

## 五、48 小时计划与赛前准备（第六节）

此为**项目管理**清单，代码库只能反映其中「工程」部分：

| 白皮书事项 | 状态 |
|------------|------|
| 确定垂类（建议 3 个） | **代码超额**：实现 6 个垂类 seed；**「官方叙事」仍可按 3 个讲** |
| 每垂类 2000–3000 真实数据采集与清洗 | **未做**（合成替代） |
| 环境、API Key | **工程已支持**；**密钥由个人配置** `.env` |
| Day1–Day2 分工任务 | **非代码** | 需团队自行对照进度 |

---

## 六、路演与提交材料（第七、十一节）

| 项目 | 状态 |
|------|------|
| 30s pitch / 8 分钟结构 / meta 演示话术 | **非代码** | 需在 Slide 与演练中准备 |
| iPad 扫码现场体验、预跑 fallback | **工程已支持**；**设备与预跑 JSON** 需人工 |
| GitHub Tag、视频、小红书笔记、Slide、一句话介绍 | **非代码** | 见 `docs/人工完成事项清单.md` |

---

## 七、合规与风险（第九、十节）

| 白皮书内容 | 状态 |
|------------|------|
| 免责声明「AI 仅供参考」 | **已做** | 报告页、导出卡片、README |
| 离线 fallback | **已做** | `frontend/src/utils/fallback.ts` |
| 真实数据采集的合法合规说明 | **未替代为代码** | 若使用真实数据，需在 README/答辩中书面说明 |
| baseline 来不及采 → 先 2 垂类 | **当前策略** | 合成 + 多垂类 demo；**与「真实数据护城河」叙事如有冲突需统一口径** |

---

## 八、工程化（白皮书未单列，仓库已加）

| 内容 | 状态 |
|------|------|
| CI（前端 build + 后端 pytest） | **已做** | `.github/workflows/ci.yml`（需保证 CI 安装 pytest） |
| Makefile（install / data / test / ci） | **已做** |
| 后端单测（baseline、文本分析等） | **已做** | `backend/tests/` |
| API 健康检查、基础日志 | **已做** | 程度见 `main.py` / orchestrator 日志 |

---

## 九、小结：答辩时建议怎么讲「完成度」

**可诚实强调的已闭环能力：** 全链路 Web 产品形态、多 Agent + 辩论时间线、量化 baseline 对比管线（结构完整）、三种上传方式、OCR、优化建议与卡片导出、工程上的 CI 与文档。

**建议主动对齐预期的缺口：** （1）**Baseline 主数据为合成**，若冲刺真实采集，需在仓库外或新脚本落地并更新叙事；（2）**CLIP 未上**；（3）白皮书写的 **Tailwind 已变为 MUI**，属技术选型更新；（4）**视频 / Slide / 参赛笔记 / Tag** 等材料需人完成。

---

**文档维护：** 大功能合并或更换数据策略后，同步更新本节「状态」列。

*生成说明：对照 `noterx_whitepaper.md` 与仓库当前实现梳理；若某文件路径变更，以仓库为准。*
